{% extends 'base.html.twig' %}

{% block title %}New Team{% endblock %}

{% block body %}
    <div class="container">

        <div class="col-6 d-flex flex-row justify-content-between align-items-center mb-3">
            <h1>{{ 'Create new team'|trans}}</h1>
            <a class=" btn btn-primary btn-sm" role="button" href="{{ path('app_teams_index') }}">{{ 'Back to list'|trans}}</a>
        </div>

    {{ include('teams/_form.html.twig') }}
    </div>

{% endblock %}
