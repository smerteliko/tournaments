{% extends 'base.html.twig' %}

{% block title %}New Tournaments{% endblock %}

{% block body %}
    <div class="container">
        <div class="col-6 d-flex flex-row justify-content-between align-items-center">
            <h1>{{ 'Create new tournament'|trans}}</h1>
            <a class="align-end btn btn-primary btn-sm" role="button" href="{{ path('app_tournaments_index') }}">{{ 'Back to list'|trans}}</a>
        </div>

        {{ include('tournaments/_form.html.twig') }}

    </div>
{% endblock %}
